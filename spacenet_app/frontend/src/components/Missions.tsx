const Missions = (props: {scenario: object, setScenario: Function, database: object, setDatabase: Function}) => {
    return (
      <h1>Welcome to the missions page!</h1>
    )
  }
  
  export default Missions